(function($, global) {
    var laurabrumm = {
        // Initialize everything
        init: function() {
            
        }
    }

    // Add window object
    global.laurabrumm = laurabrumm;

    // Run it
    laurabrumm.init();
})(jQuery, window);
